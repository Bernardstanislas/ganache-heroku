{
  "name": "ganache-heroku",
  "version": "1.0.0",
  "description": "Ganache CLI deployment on Heroku",
  "scripts": {
    "ganache": "ganache --wallet.seed yolo --server.host 0.0.0.0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ganache": "^7.8.0"
  }
}
